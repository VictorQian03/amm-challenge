#!/bin/sh
# roborev post-commit hook - auto-reviews every commit
ROBOREV="/opt/homebrew/Cellar/roborev/0.21.1/bin/roborev"
if [ ! -x "$ROBOREV" ]; then
    ROBOREV=$(command -v roborev 2>/dev/null)
    [ -z "$ROBOREV" ] || [ ! -x "$ROBOREV" ] && exit 0
fi
"$ROBOREV" enqueue --quiet 2>/dev/null &
