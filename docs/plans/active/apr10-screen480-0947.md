# Hill-Climb Run: apr10-screen480-0947

## Objective

- Start a fresh retained `screen` run from the current live `contracts/src/Strategy.sol`.
- Improve `mean_edge` for the active strategy while targeting the recorded `480` breakout on `screen`.
- Keep the coordinated retained lane owned only by the main workspace.

## Constraints

- Active eval path: `contracts/src/Strategy.sol`.
- Fresh retained lane: `artifacts/hill_climb/apr10-screen480-0947/`.
- Do not hand-edit retained artifacts.
- Keep protected mechanics untouched.
- Worker lanes must use isolated git worktrees and unique namespaced run ids under `apr10-screen480-0947`.

## Start State

- Main run id: `apr10-screen480-0947`
- Stage: `screen`
- Active path: `contracts/src/Strategy.sol`
- Live source hash at round start: `ad6e03710c508578d0f8b64ac74a262503163569c2ad16c91e1a25677eda3af1`
- Parent retained lane: `apr10-screen480-0931`
- Seed source lineage: `apr10-screen480-0931` `screen_0001`

## Evidence Snapshot

- The fresh retained baseline seeded at `473.616393`, so the line remains above the prior round but still below the new `480` breakout gate.
- Low-decile and low-retail slices remain the softest area of the scorecard, while higher-volatility and higher-retail slices already clear the breakout comfortably.
- Prior broad calm rebates, explicit fast jump-memory, hard regime rewrites, and broad inventory-skew overlays are either saturated or clearly noncompetitive in this harness.
- The next batch should focus on cheaper calm quoting that comes from better release of stale stress and sharper directionality, not from generic fee-floor cuts.

## Worker Families

| worker | run_id | hypothesis | status |
| --- | --- | --- | --- |
| w01 | `apr10-screen480-0947-w01-stale-burst-release` | Add a bounded post-burst release path that removes stale shared stress only after reconvergence and fresh-event quieting agree, targeting low-decile drag without weakening live bursts. | completed, best baseline `473.616393`; variants `473.518085` and flat |
| w02 | `apr10-screen480-0947-w02-toxic-side-burst-split` | Move more burst pricing off shared spread and onto the toxic side only when flow, divergence, and extension align, so normal regimes quote cheaper without losing continuation defense. | completed, best `462.967116` |
| w03 | `apr10-screen480-0947-w03-recovery-confidence` | Add a small recovery-confidence state from benign gaps plus reconvergence and use it to deepen healing-side relief only when stress is clearly decaying, not merely absent. | completed, best baseline `473.616393`; refinement flat |
| w04 | `apr10-screen480-0947-w04-fresh-lit-synth` | Use fresh AMM and market-making literature not cited in the prior round to design one minimal state or quote-map mutation aimed at stale-stress release or selective asymmetry. | completed, best `473.642400` |

## Decision Log

- `2026-04-10 09:47 PDT`: opened a fresh retained round from the unchanged live strategy source.
- `2026-04-10 09:48 PDT`: seeded retained baseline `screen_0001 @ 473.616393` in `apr10-screen480-0947`.
- `2026-04-10 09:49 PDT`: recorded the new outcome gate `screen >= 480.0` in retained state after re-running `set-state` to persist `outcome_gate`.
- `2026-04-10 09:55 PDT`: worker `w01` showed that post-burst shared-spread release remains directionally plausible but saturated; its first cut regressed to `473.518085` and the tighter refine only tied baseline.
- `2026-04-10 09:57 PDT`: worker `w02` confirmed that broader toxic-side burst splitting still underprices the book materially; even its repaired refine only recovered to `462.967116`.
- `2026-04-10 09:58 PDT`: worker `w03` added an explicit recovery-confidence state, but the resulting screen eval tied baseline rather than improving it, so upstream recovery memory alone does not unlock new edge.
- `2026-04-10 09:59 PDT`: worker `w04` used fresh market-making literature to narrow the idea to a continuation-gated passive recapture cut; that was the strongest raw branch at `473.642400`, still far below the `~20.04` promotion margin.
- `2026-04-10 10:00 PDT`: main-lane retained eval `screen_0002` ported the best `w04` mutation and improved raw `mean_edge` to `473.668925`, but the gain was only `+0.052532`, so the result remained `discard` and the live file was restored from `screen_0001`.
