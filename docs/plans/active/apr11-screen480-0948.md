# Hill-Climb Run: apr11-screen480-0948

## Objective

- Start a fresh retained `screen` run from the live `contracts/src/Strategy.sol`.
- Improve `mean_edge` while targeting the recorded `screen >= 480` breakout.
- Use isolated worker worktrees and worker run_ids under `apr11-screen480-0948` for parallel exploration.

## Constraints

- Active eval path: `contracts/src/Strategy.sol`.
- Main retained lane: `artifacts/hill_climb/apr11-screen480-0948/`.
- Main evals ran in clean detached worktree `apr11-screen480-0948-main` because the shared checkout had dirty protected files.
- Worker lanes used isolated worktrees and unique worker run_ids:
  - `apr11-screen480-0948-w01-arrival-band`
  - `apr11-screen480-0948-w02-reserve-skew`
  - `apr11-screen480-0948-w03-stress-slope`
  - `apr11-screen480-0948-w04-arrival-size`
- Do not hand-edit retained artifacts.
- Keep protected mechanics untouched.

## Start State

- Main run id: `apr11-screen480-0948`
- Stage: `screen`
- Live source hash at round start: `ad6e03710c508578d0f8b64ac74a262503163569c2ad16c91e1a25677eda3af1`
- Parent retained lane examined for direction: `apr10-screen480-0947`
- Baseline seeded at `screen_0001 @ 473.616393`

## Worker Families

| worker | run_id | hypothesis | result |
| --- | --- | --- | --- |
| w01 | `apr11-screen480-0948-w01-arrival-band` | Expected-arrival / gap-surprisal state plus explicit benign competition band near the normalizer. | regressed hard; best final `399.069853`; low decile fees collapsed too far |
| w02 | `apr11-screen480-0948-w02-reserve-skew` | Quiet-gated reserve-skew tie-breaker on side-specific relief only. | slight positive; `473.641283`; helped upper deciles more than the weak calm slice |
| w03 | `apr11-screen480-0948-w03-stress-slope` | Stress-derivative memory to gate healing-side rebates only after clear roll-over. | severe regression; `399.072248`; low and mid deciles broke |
| w04 | `apr11-screen480-0948-w04-arrival-size` | Separate arrival-count memory from size/toxicity memory to cheapen many-small-trade regimes. | severe regression; best final `411.271468`; calm rebates stayed too broad |
| w05 | `apr11-screen480-0948-w05-pulse-streak` | Replace latent-state carry with a pulse/streak microstructure surface that concentrates protection in same-step and short-gap clusters. | severe regression; `394.906156`; top deciles and tail protection both broke |
| w06 | `apr11-screen480-0948-w06-asymmetric-surface` | Try a simpler asymmetric state-consistent surface instead of the additive latent-state quote map. | severe regression; `391.270943`; fee jumps flattened too far |
| w07 | `apr11-screen480-0948-w07-retail-share` | Use a retail-share or benign-flow classifier to rebate only flow that looks non-toxic. | moderate regression; `467.379218`; retail edge improved slightly, but arb leakage rose and low deciles fell |
| w08 | `apr11-screen480-0948-w08-shock-spike` | Concentrate protection in thresholded shock spikes rather than steady carry. | catastrophic regression; `269.478650`; every decile regressed |

## Main-Lane Results

- `screen_0001`: retained baseline seed at `473.616393`
- `screen_0002`: continuation-gated passive recapture plus reserve-skew tie-breaker, `473.667003`, `discard`
- `screen_0003`: replay of the prior round's strongest raw continuation-gated passive-recapture branch, `473.668925`, `discard`

## Findings

- The new arrival-band and arrival-size ideas both widened calm-side competitiveness too aggressively and collapsed the weakest deciles.
- A true reserve-skew tie-breaker is directionally positive, but only as a small overlay. It does not move the soft calm slices enough by itself.
- The best raw source is still the prior continuation-gated passive recapture branch from `apr10-screen480-0947`, but its gain remains tiny relative to the `~20` screen promotion margin.
- After this round, the main retained lane has a target-stage non-improving streak of `2`, so the next continuation of this run should plan one narrower refinement before pivoting again.
- The pulse/streak, simple asymmetric, and shock-spike families are now strongly falsified for this stage; each one lost not only the weak calm slices but also meaningful tail protection.
- The retail-share classifier is the only broad pivot in this batch that showed any positive sub-signal at all, with slight retail-edge improvement, but it still worsened arb edge enough to remain clearly noncompetitive.
- No branch from `w05` to `w08` warranted promotion into the retained main lane, so the main run stays on incumbent `screen_0001` with latest retained eval `screen_0003`.
- With two failed pivot batches now recorded, the loop should pause widening and re-check evaluator evidence before starting another family-level search.

## Decision Log

- `2026-04-11 09:52 PDT`: opened fresh main run `apr11-screen480-0948` in a clean detached worktree and seeded `screen_0001 @ 473.616393`.
- `2026-04-11 09:52 PDT`: recorded breakout gate `screen >= 480`.
- `2026-04-11 09:53-09:58 PDT`: launched four isolated worker lanes covering arrival-band, reserve-skew, stress-slope, and arrival-size decompositions.
- `2026-04-11 10:00 PDT`: worker `w02` was the only positive new branch, but only by `+0.024889`; the other three worker families regressed materially by over-relaxing calm protection.
- `2026-04-11 10:03 PDT`: main-lane synthesis of continuation-gated passive recapture plus reserve-skew overlay reached `473.667003`, still a discard.
- `2026-04-11 10:04 PDT`: replayed the prior round's strongest raw branch into the fresh run; it reached `473.668925`, again a discard well below the promotion margin.
- `2026-04-11 10:05 PDT`: restored the retained incumbent into `contracts/src/Strategy.sol` and synced the main and worker run directories back under the shared repo `artifacts/hill_climb/`.
- `2026-04-11 10:14-10:24 PDT`: launched a second worker pivot batch across pulse/streak microstructure, simpler asymmetric quoting, retail-share classification, and threshold shock-spike pricing.
- `2026-04-11 10:25 PDT`: `w05` pulse/streak returned `394.906156` and `w06` asymmetric surface returned `391.270943`, ruling out those broad surface pivots for this stage.
- `2026-04-11 10:26 PDT`: `w07` retail-share classifier returned `467.379218`, the only branch with a small positive retail-edge signal, but it still leaked too much arb and regressed overall.
- `2026-04-11 10:27 PDT`: `w08` shock-spike returned `269.478650`, strongly falsifying the threshold-spike family in this basin.
- `2026-04-11 10:28 PDT`: no new worker branch was promoted into the retained main lane; synced `w05`-`w08` artifacts back to the shared repo and paused widening per the failed-pivot stop policy.
