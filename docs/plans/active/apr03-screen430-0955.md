# Hill-Climb Round: apr03-screen430-0955

## Objective

- Improve `mean_edge` for `contracts/src/Strategy.sol`.
- Optimize only for the `screen` stage in the coordinated retained lane.
- Treat `screen mean_edge >= 430` as the breakout condition.
- Start a fresh retained run from the current live strategy snapshot.

## Constraints

- Active eval path: `contracts/src/Strategy.sol`.
- Fresh retained lane: `artifacts/hill_climb/apr03-screen430-0955/`.
- Do not hand-edit retained artifacts.
- Keep protected mechanics and evaluator settings unchanged.
- Main lane is owned only by the orchestrator.
- Worker runs must use isolated git worktrees and unique namespaced run ids under `apr03-screen430-0955`.

## Baseline

- Live source hash at round start: `51bbcf7d77ec2e7ee344948e2d5e664a0a0311a7aa8ffeeed38474fc4492ea29`
- Live strategy family at round start: state-consistent danger/reversion surface with `BASE_FEE = 49` bps.
- Prior research direction comes from `apr02-screen430-2120`, but this run is fresh and retains its own lineage.

## Worker Families

| worker | run_id | hypothesis | status |
| --- | --- | --- | --- |
| w01 | `apr03-screen430-0955-w01-flow-markout-state` | Add an explicit one-sided flow-toxicity state so persistent imbalance is priced as adverse selection instead of as another cooldown tweak. | completed, best `431.020791` |
| w02 | `apr03-screen430-0955-w02-two-timescale-danger` | Split transient jump risk from slower directional toxicity so the fee floor can stay cheap while hot states widen faster. | completed, best `421.898634` |
| w03 | `apr03-screen430-0955-w03-reversion-anchor-v2` | Rebuild passive-side recapture from a cleaner quietness-plus-anchor estimate instead of layered rebates. | completed, best `442.304650` |
| w04 | `apr03-screen430-0955-w04-cheap-floor-toxic-surcharge` | Probe a lower calm floor with a bounded toxic surcharge that depends on estimated markout risk rather than raw fee ladders. | completed, best `447.695834` |
| w05 | `apr03-screen430-0955-w05-research-synth-v2` | Mine prior artifacts and external AMM dynamic fee ideas for a more principled state decomposition that reduces brittleness. | completed, best `378.537597` |
| w06 | `apr03-screen430-0955-w06-imbalance-overlay` | Keep the live surface and add a narrow flow-imbalance overlay to danger detection and side selection. | completed, best `433.480787` |
| w07 | `apr03-screen430-0955-w07-carry-shift` | Trim broad carry, strengthen reactive danger terms, and then restore a small amount of calm recapture inside the current live surface. | completed, best `448.566025` |
| w08 | `apr03-screen430-0955-w08-coherent-engine` | Replace the local reaction stack with a coherent latent-state quote engine that estimates fair value, volatility, flow, hazard, calm, and divergence before mapping state into price. | completed, best `414.938438` |

## Main Lane Results

| eval | hypothesis | result |
| --- | --- | --- |
| `screen_0001` | `baseline-live-v11` | `448.382654 seed` |
| `screen_0002` | `h001-targeted-carry-shift` | `448.566025 discard` |
| `screen_0003` | `h002-coherent-quote-engine` | `414.938438 discard` |

## Notes

- The current live file already matches the prior raw screen high-water snapshot from `apr02-screen430-2120 screen_0011`.
- The previous retained run is research input only. It is exhausted relative to its incumbent and should not be resumed.
- All retained evals in this round must stay serialized through the main lane.
- Broad structural rewrites consistently underperformed the live frontier; the successful direction was a narrow carry-to-targeted-danger rebalance inside the existing state surface.
- The best raw improvement of the round came from `w07`, which lowered broad cooldown/size carry, raised event and directional danger scaling, and then restored a small amount of calm-side recapture.
- The retained `screen` incumbent remains `screen_0001 @ 448.382654` because the improved `screen_0002` delta `+0.183371` did not clear the promotion margin `18.861403`.
- After that raw-edge refinement, the search intentionally pivoted to `h002-coherent-quote-engine`, which rewrites the strategy as a cleaner quoting engine: latent fair value, volatility, directional flow, hazard, calm, and divergence are estimated first, then mapped into shared spread plus side risk minus side opportunity.
- Worker `w08` showed that this cleaner decomposition can be tuned materially without abandoning the model: `380.355050 -> 414.938438` from `v1` to `v3`, while a more aggressive `v2` and a more convex `v4` both regressed.
- The live `contracts/src/Strategy.sol` now matches the `screen_0003` coherent-engine snapshot by design. This is a foundation pivot, not the current raw-edge frontier.
