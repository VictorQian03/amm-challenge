# Hill-Climb Round: apr01-screen420-2134

## Objective

- Improve `mean_edge` for `contracts/src/Strategy.sol`.
- Use the canonical hill-climb harness with a single retained active lane.
- Start at `screen` and only promote survivors to `climb`, `confirm`, and `final`.
- Treat the current live `LatentStateIncumbentGapAwareV4` file as the baseline for this round.

## Constraints

- Live eval path: `contracts/src/Strategy.sol` only.
- Do not modify protected mechanics, evaluator code, or stage settings.
- Do not hand-edit `artifacts/hill_climb/apr01-screen420-2134/`.
- Serialize retained evals through `uv run amm-match hill-climb ...`.
- Avoid concurrent mutation of `contracts/src/Strategy.sol` in the main working tree.
- Keep parallel subagent work read-only or isolated from the retained lane.

## Hypothesis Ledger

| label | description | source | stage | result | disposition |
| --- | --- | --- | --- | --- | --- |
| baseline | Fresh seed of the current live `LatentStateIncumbentGapAwareV4`, which matches prior `apr01-screen420-2122 screen_0004` at `428.250099`. | main lane + prior retained evidence | screen | `screen_0001 seed @ 428.250099` | seeded |
| baseline-promoted | Promote the screened `LatentStateIncumbentGapAwareV4` incumbent to `climb` in the coordinated retained lane. | main lane | climb | `climb_0003 seed @ 428.172691` | seeded |
| baseline-confirm | Promote the coordinated-lane `LatentStateIncumbentGapAwareV4` incumbent from `climb` to `confirm`. | main lane | confirm | `confirm_0004 seed @ 415.322533` | seeded |
| baseline-final | Promote the coordinated-lane `LatentStateIncumbentGapAwareV4` incumbent from `confirm` to `final`. | main lane | final | `final_0005 seed @ 420.612993` | seeded |
| clustered-pulse-overlay | Add a very short-lived same-side pulse only for same-step or gap-`<= 1` clustered shocks, so toxic bursts widen harder without raising calm carry. | prior `pulse-pivot` salvage + microstructure intuition | screen | `screen_0002 discard @ 428.250099` (`delta +0.000000`, margin `18.704658`) | invalidated |
| idle-sidecar-recapture | Keep the `V4` defense core, but add a narrower idle-gap rebate on the non-toxic side only when `cooldown`, `shock`, and `defense` are all quiet. | prior `quiet-gap-release` miss analysis + local synthesis | screen | pending | pending |
| inventory-lean-lite | Add a capped reserve-skew overlay only as a tie-breaker during calm-to-moderate states, favoring inventory-restoring flow without the broad penalty that hurt `reserve-skew-maker`. | prior discard analysis + AMM fee literature | screen | pending | pending |
| price-vol-memory | Add a separate fast-decay price-jump memory so repeated markout risk lifts common fees even when raw trade size is moderate, then decays faster than cooldown after gaps. | prior `shock-decomposition` evidence + volatility-sensitive fee literature | screen | pending | pending |
| stale-tilt-on-danger | Replace the always-on `+1 bps` stale-side tilt with a stronger but danger-gated latent-gap tilt that activates only when latent gap and cooldown jointly imply adverse selection risk. | prior `latent-gap-tilt` / `regime-gap-shock` analysis | screen | pending | pending |

## Notes

- Breakout target for this round: `screen mean_edge >= 420`.
- Fresh retained active lane for this round: `artifacts/hill_climb/apr01-screen420-2134/`.
- The current live `Strategy.sol` matched prior `two-regime-defense` when the run was seeded; `screen_0001` confirmed that stronger `428.250099` baseline.
- The official coordinated lane has now promoted that same snapshot to `climb`, where it seeded at `428.172691`.
- The same retained snapshot then seeded `confirm` at `415.322533` and `final` at `420.612993`.
- `screen_0002` recorded the same snapshot hash and strategy name as the baseline, so the retained eval did not exercise a distinct candidate despite the label. Treat that hypothesis as untested and restore from `pull-best` before any future screening.
- Parallel side-lab retained runs may exist from delegated workers, but `apr01-screen420-2134` is the only coordinated lane for this round.
- Parallel subagents are generating additional mutation ideas from prior ledgers, candidate-library salvage, and external AMM literature; fold only selected ideas into the main lane.
