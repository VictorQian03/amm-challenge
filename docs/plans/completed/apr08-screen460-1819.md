# Hill-Climb Run: apr08-screen460-1819

## Objective

- Seed a fresh retained `screen` run from the best raw refinement discovered under `apr08-screen460-1813`.
- Keep the coordinated lane on the strongest verified source after the prior run reached the `refine now` trigger.

## Start State

- Main run id: `apr08-screen460-1819`
- Seed source lineage: `apr08-screen460-1813` `screen_0003`
- Source sha at seed: `d70403f1a547994cba1c2530b420c6434657f3f6d5f9db0c1e32809161ec5eca`
- Parent retained lane: `apr08-screen460-1813`

## Rationale

- `apr08-screen460-1813` hit the `3`-miss refine trigger exactly as expected.
- Within that line, the best raw source came from combining a narrow quiet divergence release with a slightly more directional above-threshold burst split.
- Since the screen promotion margin prevents that source from replacing the incumbent inside the same run, the right retained action is to reseed a fresh run from the stronger source.
