# apr21-screen490-1413

Superseded on 2026-04-21 after the evaluator repair in `amm_sim_rs` required a fresh retained `run_id`. The active canonical lane is now `apr21-screen490-1431`.

## Objective

- Start a fresh retained `screen` lane and push the canonical `screen` incumbent above the breakout target of `490.0` mean edge.
- Keep the canonical retained lane serialized in the main workspace.
- Use isolated worker worktrees for parallel exploration so no worker mutates shared retained artifacts.

## Constraints

- Use the thin harness only for evaluation and read surfaces; planning lives here.
- Seed the lane from `contracts/src/StarterStrategy.sol`; the mutable strategy surface in this revision is still `StarterStrategy.sol`.
- Canonical retained run: `apr21-screen490-1413`.
- Historical note: this round used distinct worker `run_id`s, but future worker exploration should use `hill-climb probe` instead of creating retained worker lanes.
- Promotion on the canonical lane still depends on the harness margin, not the breakout target alone.

## Baseline

- Seed eval: `screen_0001`
- Label: `starter-baseline`
- Mean edge: `485.92377070367183`
- Gap to breakout target: `4.07622929632817`
- Retail edge: `539.6863705273314`
- Arb edge: `-53.76259982365966`
- `arb_loss_to_retail_gain`: `0.09961822784430861`
- `quote_selectivity_ratio`: `21.366983876018754`
- `time_weighted_mean_fee`: `0.004662250340166877`
- `max_fee_jump`: `0.05949151764735314`
- Tail reads:
  - `low_decile_mean_edge`: `370.69865470550553`
  - `low_retail_mean_edge`: `415.9137203431734`
  - `low_volatility_mean_edge`: `463.32099611706855`

## Working Thesis

The baseline is already near the `490` breakout gate. The weakest read is the downside floor, not the headline average. We should spend the first branch portfolio on structural tail-floor improvements that do not simply flatten the entire quote engine.

## Parallel Lanes

### Worker 1

- Branch: `codex/apr21-screen490-1413-w1-tailfloor`
- Worktree: `/Users/victorqian/Desktop/opt_arena/simple_amm_worktrees/apr21-screen490-1413-w1-tailfloor`
- Worker run id: `apr21-screen490-1413-w1-tailfloor`
- Ownership: tail-floor lift
- Focus:
  - Raise `low_decile_mean_edge` / `low_retail_mean_edge`
  - Prefer quiet-regime recentering or downside protection changes
  - Avoid broad fee smoothing that weakens the incumbent's high-decile edge

### Worker 2

- Branch: `codex/apr21-screen490-1413-w2-flowtox`
- Worktree: `/Users/victorqian/Desktop/opt_arena/simple_amm_worktrees/apr21-screen490-1413-w2-flowtox`
- Worker run id: `apr21-screen490-1413-w2-flowtox`
- Ownership: continuation-toxicity handling
- Focus:
  - Reduce arb leakage under one-sided flow continuation
  - Improve `arb_loss_to_retail_gain` without collapsing `quote_selectivity_ratio`
  - Treat `max_fee_jump` as a diagnostic, not a blanket smoothing target

### Worker 3

- Branch: `codex/apr21-screen490-1413-w3-quietgap`
- Worktree: `/Users/victorqian/Desktop/opt_arena/simple_amm_worktrees/apr21-screen490-1413-w3-quietgap`
- Worker run id: `apr21-screen490-1413-w3-quietgap`
- Ownership: burst-stress / short-gap handling
- Focus:
  - Target the short-gap or jumpy regime rather than another calm-state recentering variant
  - Improve low-volatility or burst-stress behavior without generic smoothing
  - Watch for selectivity collapse while addressing `max_fee_jump` as a diagnostic

## Worker Rules

- Each worker may edit only `contracts/src/StarterStrategy.sol` in its own worktree.
- Each worker should run:
  - `uv run amm-match validate contracts/src/StarterStrategy.sol`
  - `uv run amm-match hill-climb probe --stage screen contracts/src/StarterStrategy.sol`
- Each worker should report:
  - Exact mean edge delta vs baseline
  - Tail metrics deltas
  - Whether the result looks promotable to the canonical lane
  - Any regression risk that should block canonical spend

Future guardrail:
Only the selected branch should be rerun into the canonical retained lane with `hill-climb eval`; worker worktrees should not leave retained artifact directories behind.

## Canonical Next Step

Once the first worker results return, compare them against the seeded baseline and spend at most one canonical follow-up eval on the strongest structurally distinct branch.

## Round 0 Outcome

- Reviewer correction:
  - Worker 3 was retargeted away from calm-state overlap and onto burst-stress / short-gap handling.
- Worker 1 result:
  - Change: second quiet-gap recenter for confirmed calm regimes
  - Validation: passed
  - Worker-local screen eval: failed with `Simulation result is missing required field 'retail_edge'`
  - Canonical decision: do not spend
- Worker 2 result:
  - Change: continuation-aligned leakage guard now also suppresses safe-side recapture or refill paths
  - Validation: passed
  - Worker-local screen eval: failed with `Simulation result is missing required field 'retail_edge'`
  - Canonical decision: do not spend
- Worker 3 result:
  - Change: short-gap burst signal now feeds the existing event surcharge path
  - Validation: passed
  - Worker-local screen eval: failed with `Simulation result is missing required field 'retail_edge'`
  - Canonical decision: do not spend

## Current Canonical State

- Canonical retained lane remains `apr21-screen490-1413`
- Canonical incumbent remains `screen_0001` at `485.92377070367183`
- No canonical follow-up eval has been spent after the seed
- Immediate blocker before spending the next canonical eval: understand why structurally valid worker branches are producing scorecards without `retail_edge`
