# Hill-Climb Run: apr08-screen470-2318

## Objective

- Start a fresh retained `screen` run from the best raw refinement discovered under `apr08-screen470-2242`.
- Keep chasing the recorded breakout gate at `470` mean edge on `screen`.
- Reset the retained comparison baseline to the stronger normal-regime compression source.

## Start State

- Main run id: `apr08-screen470-2318`
- Seed source lineage: `apr08-screen470-2242` `screen_0004`
- Source sha at seed: current `contracts/src/Strategy.sol` after promoting the worker-best normal-regime compression pass
- Parent retained lane: `apr08-screen470-2242`

## Rationale

- `apr08-screen470-2242` reached the `refine now` trigger exactly as expected after three non-improving screen evaluations.
- Within that lane, the best raw source came from a modest normal-regime fee compression: slightly lower base fee, unchanged one-sided flow carry, and a stronger event ramp.
- The screen promotion margin prevented that source from replacing the incumbent inside the same run, so the retained action is to reseed a fresh run from the stronger raw source and continue searching above `463.167329`.
