# Hill-Climb Run: apr17-screen480-0834

## Objective

- Start a fresh retained `screen` run on the current evaluator fingerprint and preserve the `screen >= 480` breakout gate.
- Stop after idea generation, baseline seeding, and queueing the first hypothesis. Do not spend a new mutation eval yet.
- Force a real layer/topology pivot after the prior lane collapsed into additive-spine replays.

## Constraints

- Active eval path: `contracts/src/Strategy.sol`.
- Fresh retained lane: `artifacts/hill_climb/apr17-screen480-0834/`.
- Baseline on the new lane: `screen_0001 @ 473.616393`.
- Prior lane `apr13-screen480-0907` is read-only planning history.
- Do not inspect `contracts/src/Reference.sol`.
- Do not hand-edit retained artifacts.

## Evidence Read

- `artifacts/index.json` and `artifacts/INDEX.md` show no active retained hill-climb lane; `apr13-screen480-0907` is the latest historical read surface.
- `analyze-run` on `apr13-screen480-0907` reports `force_layer_pivot=true` and groups four of the five failed branches inside the same `additive-incumbent-spine` phenotype family.
- `screen_0005` was the strongest raw survivor, but it improved `mean_edge` by only `+0.565591` versus `screen_0001` and still missed the promotion margin as a near-replay local refinement.
- `prescreen_0008` beat `prescreen_0007` on `mean_edge` and `low_retail_mean_edge`, but it still behaved like a hidden cheap mode and failed the gate, so topology separation must stay defensive by default.
- The retained memo says the binding weakness remains calm / low-retail / weak-decile performance, while the main mechanical failure is noisy over-spiky fee motion rather than simply average fee level.

## Batch Diagnosis

- Incumbent decomposition:
  - state estimation: fast event-driven latent toxicity and carry memory
  - risk budget: symmetric defensive widening with side-sensitive overlays
  - opportunity budget: limited calm relief layered onto the same broad surface
  - quote map: one additive assembly spine that mixes defense and opportunity spend
- Main trap: recent branches changed labels but kept the same hidden coupling, where the same additive surface still decides both the persistent defensive floor and when calm relief is allowed. That produced either tiny near-replays (`screen_0003` / `screen_0005`) or broad cheap-mode failures (`prescreen_0007` / `prescreen_0008`).
- Failure mode: mixed phenotype collapse and decomposition-label collapse. The batch nominally touched all four layers, but most branches still expressed the same additive quote topology and therefore converged to the same failure family.

## Hypotheses

1. `baseline-split-side-protection`
   - Primary layer changed: `quote_map`
   - Layer held fixed: `state`
   - Quote topology: `baseline-plus-side-specific protection`
   - Phenotype family: `multi-channel quote assembly`
   - Mutation family: `two_channel_spread_split`
   - Novelty coordinates: `{"classification":"novel","external_idea":"myersonian_spread_split","intent":"structural_pivot"}`
   - Hidden coupling removed: the incumbent uses one additive surface to set both the symmetric defensive floor and the toxic-side surcharge.
   - Evidence anchor: reacts to the `screen_0005` near-replay and the `prescreen_0008` cheap-mode router failure.
   - Anti-target phenotype: if calm windows still cheapen both sides together, this is only another additive replay.
   - Core idea: assemble quotes from two channels: a persistent symmetric baseline floor and a separate side-specific protection channel that can widen only the toxic side. The baseline releases slowly from confirmed calm, while the toxic channel remains responsive to one-sided evidence.
   - Why this is structurally different: it changes quote assembly itself instead of retuning one shared surface.
   - Expected upside: improve calm and low-retail slices without reopening broad arb leakage.
   - Expected failure signature: low-retail improves, but `arb_loss_to_retail_gain` or average fee spend regresses because the channel split is still too permissive.

2. `filtered-cluster-state`
   - Primary layer changed: `state`
   - Layer held fixed: `quote_map`
   - Quote topology: `single-surface additive`
   - Phenotype family: `state-filtered additive control`
   - Mutation family: `cluster_persistence_state`
   - Novelty coordinates: `{"classification":"novel","external_idea":"volatility_cluster_persistence","intent":"fee_discipline"}`
   - Hidden coupling removed: instantaneous shocks currently drive both volatility estimation and release timing.
   - Evidence anchor: reacts to `screen_0002` plus the retained memo’s recommendation for filtered volatility floors.
   - Anti-target phenotype: if single-tick shocks still dominate the response and `max_fee_jump` barely moves, the branch is a replay.
   - Core idea: replace the current event-spike interpretation with a filtered cluster-persistence state that only elevates protection after repeated aligned stress, then decays with memory instead of abrupt relaxation. Keep the incumbent quote map fixed so the experiment isolates state semantics.
   - Why this is structurally different: it changes what the controller believes about market state, not the coefficients applied after the fact.
   - Expected upside: smoother protection with less over-spiky fee motion and less accidental calm cheapening.
   - Expected failure signature: over-smoothing flattens tail defense and recreates the arb leak seen in the first fee-discipline branch.

3. `earned-calm-credit-ledger`
   - Primary layer changed: `opportunity_budget`
   - Layer held fixed: `risk_budget`
   - Quote topology: `single-surface additive`
   - Phenotype family: `budgeted release control`
   - Mutation family: `earned_release_ledger`
   - Novelty coordinates: `{"classification":"novel","external_idea":"calm_credit_ledger","intent":"weak_slice"}`
   - Hidden coupling removed: calm relief is currently granted by the same contemporaneous signals that decide broad protection, so spend persists longer than evidence supports.
   - Evidence anchor: reacts to `prescreen_0007`, which cheapened too broadly while trying to help weak slices.
   - Anti-target phenotype: if relief behaves like a continuous discount across both sides, the branch is only a renamed weak-slice replay.
   - Core idea: make calm competitiveness an earned and depleting budget that accumulates only after repeated benign confirmations, can be spent only in short windows, and decays aggressively after use. Keep the current risk budget fixed to isolate how opportunity spend is earned and exhausted.
   - Why this is structurally different: it changes the spend ledger and release semantics instead of softening thresholds on the same overlay.
   - Expected upside: targeted low-retail and low-decile improvement without leaving the book cheap all the time.
   - Expected failure signature: weak slices rise, but arb leakage and tail loss return because credits refill too easily.

4. `hazard-gated-side-regime`
   - Primary layer changed: `risk_budget`
   - Layer held fixed: `opportunity_budget`
   - Quote topology: `regime-switched quote assembly`
   - Phenotype family: `regime-switched quote assembly`
   - Mutation family: `hazard_side_gate`
   - Novelty coordinates: `{"classification":"novel","external_idea":"side_hazard_gate","intent":"anti_arb"}`
   - Hidden coupling removed: one controller currently decides both when the book should become defensive and how one-sided toxicity should be charged.
   - Evidence anchor: reacts to `screen_0006` and `prescreen_0008`, both of which mixed selective toxicity control with broad mode changes.
   - Anti-target phenotype: if the regime switch still creates a hidden cheap mode or both sides move together, it is only a softer router replay.
   - Core idea: keep opportunity spend fixed, but let risk budgeting switch between a defensive symmetric regime and a one-sided hazard regime only when toxicity evidence aligns with side and extension. The switch never opens a broad competitive mode; it only changes the geometry of risk protection.
   - Why this is structurally different: it introduces a risk-semantic regime switch rather than another additive overlay or cheap router.
   - Expected upside: better one-sided arb defense without sacrificing the incumbent calm floor.
   - Expected failure signature: regime flapping or symmetric overpricing erases calm-slice gains.

## Entropy Check

- Reused quote topologies: `single-surface additive` appears in hypotheses 2 and 3 only, with different primary layers and mutation families.
- Reused phenotype families: none from the exhausted `additive-incumbent-spine` family.
- New quote topologies vs exhausted batch: `baseline-plus-side-specific protection`, `regime-switched quote assembly`.
- New external ideas vs exhausted batch: `myersonian_spread_split`, `volatility_cluster_persistence`, `calm_credit_ledger`, `side_hazard_gate`.
- Why this batch should not collapse into one phenotype: the first and fourth branches change quote assembly, the second isolates state semantics while holding quote assembly fixed, and the third isolates opportunity spend accounting. The batch therefore spans four primary layers and two genuinely new topologies instead of replaying one additive spine.

## Execution Order

1. `baseline-split-side-protection`
2. `filtered-cluster-state`
3. `earned-calm-credit-ledger`
4. `hazard-gated-side-regime`

## Queue Decision

- Queue `baseline-split-side-protection` first.
- Reason: it directly addresses the hidden coupling identified by both the near-replay (`screen_0005`) and the failed router pivot (`prescreen_0008`), while staying more defensively bounded than another cheap-mode topology experiment.

## Execution Log

- `2026-04-17 08:34 PDT`: reviewed `artifacts/index.json`, `artifacts/INDEX.md`, `docs/agent_harness_guide.md`, `docs/hill_climb_loop.md`, `docs/codex_idea_generation_prompt.md`, the `apr13-screen480-0907` analysis surfaces, and the linked research memo.
- `2026-04-17 08:36 PDT`: seeded the fresh retained lane with `screen_0001 @ 473.616393` from the current baseline `contracts/src/Strategy.sol`.
- `2026-04-17 08:38 PDT`: registered batch `apr17-screen480-batch1`, queued `baseline-split-side-protection`, and recorded the `screen >= 480` breakout gate in run state.
- `2026-04-17 08:46 PDT`: implemented `baseline-split-side-protection` as a true two-channel quote-map rewrite, validated `contracts/src/Strategy.sol`, and recorded `screen_0002 @ 478.073051` as a discard. The branch improved `mean_edge` by `+4.456658`, lifted low-retail and low-volatility slices, improved `arb_loss_to_retail_gain` to `0.122159`, and reduced `max_fee_jump` to `0.054570`, but it missed the `19.992916` promotion margin.
- `2026-04-17 08:47 PDT`: registered child hypothesis `baseline-split-side-protection-v2`, shifting more spend from the shared floor into the toxic-side overlay and allowing only a tightly capped skim of baseline flex on the non-toxic side.
- `2026-04-17 08:48 PDT`: validated and recorded `screen_0003 @ 478.863005` for `baseline-split-side-protection-v2` as another discard. It beat `screen_0002` on `mean_edge`, low-retail, low-volatility, and time-weighted mean fee, but the incremental `+0.789954` versus `screen_0002` still left the run well short of the `20.037665` promotion margin over the incumbent.
- `2026-04-17 08:49 PDT`: registered child hypothesis `baseline-split-side-protection-v3`, preserving the capped skim discipline while pushing more event and hazard spend into the toxic-side overlay.
- `2026-04-17 08:49 PDT`: validated and recorded `screen_0004 @ 483.890738` for `baseline-split-side-protection-v3` as the strongest raw branch in the lane so far. It improved `mean_edge` by `+10.274345`, tightened `arb_loss_to_retail_gain` to `0.103836`, and lifted low-retail to `414.218176`, but it paid for that with higher `time_weighted_mean_fee` (`0.004632`) and a small retail-edge giveback, so it still missed the `20.250296` promotion margin and remained a discard.
- `2026-04-17 08:50 PDT`: left `contracts/src/Strategy.sol` on the `screen_0004` source because the harness now explicitly recommends another same-line refine before any pivot; the next useful move is to reclaim some retail/fee spend from the stronger toxic overlay instead of abandoning the two-channel line blindly.
- `2026-04-17 08:50 PDT`: registered `baseline-split-side-protection-v4` as the queued next move and updated run state accordingly. The evidence-backed next step is to preserve most of `screen_0004`'s raw edge and arb discipline while reclaiming retail edge and fee spend through a narrower non-toxic recovery path.
- `2026-04-17 08:58 PDT`: re-read the canonical harness surfaces and confirmed the retained state had already advanced past the stale `baseline-split-side-protection-v4` note: `screen_0005`, `screen_0006`, and the failed state pivot `screen_0007` were already recorded, and `hazard-gated-side-regime` was the queued next hypothesis in `state.json`.
- `2026-04-17 09:09 PDT`: restored `screen_0001` into `contracts/src/Strategy.sol`, implemented `hazard-side-gate-v1`, and recorded `screen_0008 @ 473.616393` as a discard for `hazard-gated-side-regime`. The source hash changed, but the full screen scorecard matched the incumbent exactly, so the branch behaved as a functional no-op rather than a live regime pivot.
- `2026-04-17 09:10 PDT`: restored the retained `screen_0001` incumbent via `pull-best`, marked `earned-calm-credit-ledger` as the next queued hypothesis, and added a pause note to reread the lane before spending the remaining weak-slice pivot because the run has now reached its configured `stop_after_non_improving_iterations = 8` limit.
- `2026-04-17 09:17 PDT`: re-read `status`, `analyze-run`, and the live hypothesis registry before spending the final open branch. The lane was resumable, `earned-calm-credit-ledger` was the only remaining open hypothesis, and the evidence still pointed to `screen_0007`-style broad cheapening as the main anti-target.
- `2026-04-17 09:18 PDT`: implemented `earned-calm-credit-v1` as an opportunity-budget-only pivot: kept the incumbent risk path intact, shrank the continuous shared calm rebate, and added an earned calm-credit balance plus benign-confirm streak that could only spend on the aligned non-toxic side. `uv run amm-match validate contracts/src/Strategy.sol` passed before the eval.
- `2026-04-17 09:18 PDT`: recorded `screen_0009 @ 460.338827` for `earned-calm-credit-ledger` as a discard. The branch lowered `time_weighted_mean_fee` to `0.004114`, reduced `max_fee_jump` to `0.054400`, and held `retail_edge` roughly flat, but it materially worsened `arb_edge` to `-80.495709`, pushed `arb_loss_to_retail_gain` up to `0.148836`, and collapsed the weak slices (`low_retail_mean_edge = 388.560544`, `low_decile_mean_edge = 286.991835`). The harness tagged it as `arb_leak_regression`, matching the hypothesis’s expected failure mode.
- `2026-04-17 09:19 PDT`: restored the incumbent `screen_0001` source via `pull-best` and cleared the stale queued next hypothesis from `state.json`. The run now has no open hypotheses, `analyze-run` reports `selection_mode = none`, and the canonical guidance is to stop this batch and seed a new one with forced layer/topology diversity rather than spend another same-family replay.
