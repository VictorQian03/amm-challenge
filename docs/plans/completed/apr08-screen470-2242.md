# Hill-Climb Run: apr08-screen470-2242

## Objective

- Start a fresh retained `screen` run from the current `contracts/src/Strategy.sol` line.
- Chase the recorded breakout gate at `470` mean edge on `screen`.
- Keep the coordinated retained lane owned only by the main workspace.

## Main Lane

- Main run id: `apr08-screen470-2242`
- Stage: `screen`
- Active path: `contracts/src/Strategy.sol`
- Seed lineage: current main-branch strategy from retained run `apr08-screen470-2222` `screen_0001`

## Worker Lanes

- `apr08-screen470-2242-w01-transient-toxicity`
  - Test whether a lighter but more selective transient toxicity memory can widen fees only during clustered harmful flow instead of taxing common flow broadly.
- `apr08-screen470-2242-w02-flow-persistence`
  - Test whether persistent one-sided flow should push more side-specific protection only when divergence and hazard already agree.
- `apr08-screen470-2242-w03-selective-recapture`
  - Test whether calm-state recapture can shift from simple long-gap gating toward profit-region style side recapture after reconvergence without broad rebates.

## Decision Notes

- The last retained lane plateaued on quiet-gap recenter and selective passive-recapture refinements with gains far below the screen promotion margin.
- The next search round should avoid another broad inventory-skew overlay and should instead probe unsaturated state-separation ideas with clearer regime gating.
- Worker evals stay isolated in worker-specific worktrees and worker-specific run directories; only the main lane may write to `apr08-screen470-2242`.

## Round 2 Notes

- Online research and retained eval review both point toward threshold-style dynamic fees that stay stable in normal conditions and react sharply only when volatility or divergence becomes materially large.
- The current incumbent is already strong in stressed buckets and weak in low-intensity buckets, so the next round should reduce always-on common carry before trying any further benign-state rebates.
- New worker lanes should focus on:
  - directional fees for swaps that push price farther away from the latent center versus trades that pull it back,
  - thresholded or quadratic divergence carry instead of another linear low-grade tax,
  - a more competitive normal regime with sharper event gating rather than broad passive recapture.
