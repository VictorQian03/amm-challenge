# Hill-Climb Run: apr17-screen480-0928

## Objective

- Start a fresh retained `screen` run on the current evaluator fingerprint and preserve the `screen >= 480` breakout gate.
- Stop after idea generation, baseline seeding, and queueing the first hypothesis. Do not spend a new mutation eval yet.
- Force a real family pivot away from the exhausted `two_channel_spread_split` / `baseline-plus-side-specific-protection` line.

## Constraints

- Active eval path: `contracts/src/Strategy.sol`.
- Fresh retained lane: `artifacts/hill_climb/apr17-screen480-0928/`.
- Prior lane `apr17-screen480-0834` is read-only planning history and is already stopped by streak policy.
- Do not inspect `contracts/src/Reference.sol`.
- Do not hand-edit retained artifacts.

## Evidence Read

- `artifacts/index.json` and `uv run amm-match hill-climb analyze-run --run-id apr17-screen480-0834 --json --read-only` show the latest lane is historical and exhausted, with `selection_mode = none`, no open hypotheses, and a forced layer pivot recommendation.
- The strongest raw branch in the exhausted lane was `screen_0004 @ 483.890738`, but five attempts in mutation family `two_channel_spread_split` produced zero survivors and now count as a dead end.
- `screen_0004` improved `arb_edge`, `arb_loss_to_retail_gain`, `low_retail_mean_edge`, and `low_volatility_mean_edge`, but it paid with higher average fee and small retail-edge giveback, so the line is useful as a phenotype anchor rather than a branch to replay.
- `screen_0009` cut `time_weighted_mean_fee` and `max_fee_jump`, but it reopened arb leakage and collapsed weak slices, proving that broad calm cheapening is still the main anti-target.
- The retained research memo still points to filtered volatility floors and a decomposition between persistent symmetric protection and asymmetric adverse-selection charging as the strongest unspent evidence-backed direction.

## Incumbent Decomposition

- State estimation: fast event-driven toxicity and carry memory react quickly but release too directly from contemporaneous calm.
- Risk budget: one defensive controller pays for both persistent floor protection and one-sided hazard response.
- Opportunity budget: calm relief exists, but it is still coupled to the same short-horizon evidence that decides protection.
- Quote map: a mostly shared additive surface still dominates final quote assembly even when labels imply separate channels.

## Benchmark Decomposition

- State estimation: more persistence-aware than the incumbent, with enough memory to distinguish repeated stress from alternating noise.
- Risk budget: keeps a persistent protective floor while still charging one-sided toxicity more selectively than the incumbent.
- Opportunity budget: recovers competitiveness narrowly in calm windows instead of opening a broad cheap regime.
- Quote map: separates baseline protection, side-specific adverse-selection response, and selective recovery more cleanly than the incumbent.

## Local-Optimum Trap

The local optimum is no longer just "shared additive spine" in name. The actual trap is that recent branches still let one short-horizon control path decide three things at once: the persistent symmetric floor, the one-sided toxicity surcharge, and the release of calm competitiveness. The raw-improving split-side line proved there is real signal in stronger anti-arb protection, but five iterations also proved that the same family cannot clear the noise margin once average fee and retail drag are paid. The next lane therefore needs a real semantic pivot in state or budgeting, plus at least one quote-assembly branch that is not another renamed floor-plus-overlay replay.

## Failure Diagnosis

This is a mixed failure, but the dominant mode is phenotype collapse reinforced by decomposition-label collapse. The exhausted lane nominally touched all four layers, yet every branch still expressed the same broad phenotype: protection and calm recovery remained too tightly coupled, and the search never produced a live alternative assembly that could be both selective and margin-clearing. There is not a harness-evidence gap anymore. The retained read surfaces are sufficient; the problem is that the next batch must use them to force a stronger semantic split between persistent protection, fast toxicity charging, and narrowly gated recovery.

## Hypotheses

1. `vol-accumulator-side-tax`
   - Primary layer changed: `state`
   - Layer held fixed: `opportunity_budget`
   - Quote topology: `two-timescale quote assembly`
   - Phenotype family: `multi-timescale risk control`
   - Mutation family: `vol_accumulator_floor`
   - Novelty coordinates: `{"classification":"novel","external_idea":"filtered_volatility_floor","intent":"fee_discipline"}`
   - Hidden coupling removed: instantaneous shocks should not decide both protective-floor activation and release timing.
   - Evidence anchor: reacts to `screen_0004` as the best anti-arb phenotype and `screen_0007` as the fee-disciplined branch that became too cheap.
   - Anti-target phenotype: if small alternating shocks still pull the whole book cheap or wide together, this is only another replay of shared short-horizon control.
   - Core idea: replace the incumbent's short-lived event carry with a slower filtered volatility accumulator that sets the symmetric baseline floor, while a faster side-tax path still reacts to one-sided toxicity. Keep the opportunity path fixed so the experiment isolates whether two timescales in state semantics can preserve anti-arb improvements without paying the full retail and fee penalty of the dead split-side family.
   - Why this is structurally different: it changes what the controller believes about persistent stress before quote assembly is applied.
   - Expected upside: smoother protective floors with less accidental cheapening and better odds of retaining the `screen_0004` anti-arb gain.
   - Expected failure signature: mean edge stays flat because the slow floor lags true stress, or arb leakage returns because the fast side tax is too muted.

2. `drawdown-budget-shell`
   - Primary layer changed: `risk_budget`
   - Layer held fixed: `state`
   - Quote topology: `three-zone quote assembly`
   - Phenotype family: `budgeted risk shell`
   - Mutation family: `drawdown_budget_shell`
   - Novelty coordinates: `{"classification":"novel","external_idea":"loss_budget_shell","intent":"anti_arb"}`
   - Hidden coupling removed: the current controller spends the same risk budget on persistent protection and one-sided stress response with no explicit budget accounting.
   - Evidence anchor: reacts to the anti-arb upside of `screen_0004` and the no-op failure of `screen_0008`.
   - Anti-target phenotype: if both sides still move nearly in lockstep during stress, the branch has recreated the same hidden shared budget.
   - Core idea: allocate risk into three explicit zones: a small always-on floor, a stress shell that thickens when recent arb loss or toxic flow consumes budget, and a one-sided extension shell that can widen only on the toxic side. The budget refills slowly after protected intervals instead of relaxing continuously with every calm tick.
   - Why this is structurally different: it introduces explicit risk accounting rather than another regime toggle or additive surcharge retune.
   - Expected upside: preserve one-sided protection without paying the full symmetric fee tax that blocked promotion in the split-side line.
   - Expected failure signature: the shell stays too conservative and average fee drifts upward without enough incremental edge to clear margin.

3. `recovery-corridor-auction`
   - Primary layer changed: `opportunity_budget`
   - Layer held fixed: `risk_budget`
   - Quote topology: `non-toxic-side recovery corridor`
   - Phenotype family: `selective recovery control`
   - Mutation family: `recovery_corridor_auction`
   - Novelty coordinates: `{"classification":"novel","external_idea":"selective_recovery_corridor","intent":"weak_slice"}`
   - Hidden coupling removed: calm recovery should not be earned and spent as a broad global discount path.
   - Evidence anchor: reacts directly to the `screen_0009` arb-leak regression.
   - Anti-target phenotype: if calm relief appears on both sides or persists through renewed stress, the branch is only a renamed cheap-mode replay.
   - Core idea: keep the incumbent risk path fixed, but replace global calm credit with a narrow recovery corridor that can reduce only the non-toxic side and only when repeated calm confirmations outnumber recent toxic events by a clear margin. The corridor closes immediately when toxicity returns and does not bank spend across long windows.
   - Why this is structurally different: it changes the geometry of where recovery can appear, not just how much rebate is accumulated.
   - Expected upside: targeted low-retail and low-volatility relief without reopening the broad arb leak seen in the earned-credit line.
   - Expected failure signature: weak slices improve slightly but mean edge stalls because the corridor is too narrow to matter, or arb leakage returns because corridor entry is too permissive.

4. `inventory-toxicity-triad`
   - Primary layer changed: `quote_map`
   - Layer held fixed: `opportunity_budget`
   - Quote topology: `three-channel quote assembly`
   - Phenotype family: `tri-channel quote assembly`
   - Mutation family: `inventory_toxicity_triad`
   - Novelty coordinates: `{"classification":"novel","external_idea":"inventory_tax_blend","intent":"structural_pivot"}`
   - Hidden coupling removed: the incumbent still lets one broad surface absorb baseline defense, toxic charging, and inventory-centering pressure.
   - Evidence anchor: reacts to the full `two_channel_spread_split` dead end and to the retained benchmark debrief that warns against treating jump size alone as quality.
   - Anti-target phenotype: if the final quote still behaves like one smoothed additive envelope, the branch is only a disguised replay.
   - Core idea: assemble the quote from three channels with explicit roles: persistent symmetric floor, toxic-side adverse-selection tax, and inventory-centering offset that can only act when it does not fight the toxic signal. Keep opportunity spend fixed so the experiment isolates whether a cleaner tri-channel assembly can retain selectivity without the retail drag that blocked the two-channel family.
   - Why this is structurally different: it changes quote assembly itself and introduces a third orthogonal channel instead of reweighting the dead two-channel split.
   - Expected upside: a cleaner structural pivot that can keep anti-arb gains while reducing unnecessary symmetric fee spend.
   - Expected failure signature: inventory centering fights toxicity and recreates either broad overpricing or silent cheapening.

## Entropy Check

- Reused quote topologies: none.
- Reused phenotype families: none from the exhausted lane.
- New quote topologies vs exhausted batch: `two-timescale quote assembly`, `three-zone quote assembly`, `non-toxic-side recovery corridor`, `three-channel quote assembly`.
- New external ideas vs exhausted batch: `filtered_volatility_floor`, `loss_budget_shell`, `selective_recovery_corridor`, `inventory_tax_blend`.
- Why this batch should not collapse into one phenotype: the first branch changes state semantics, the second changes explicit risk accounting, the third constrains recovery geometry, and the fourth changes quote assembly itself. None reuse the dead `baseline-plus-side-specific-protection` spine, and only one branch is a quote-map-first topology pivot.

## Best Next Batch

- Recommend the first 2 hypotheses to test: `vol-accumulator-side-tax`, then `drawdown-budget-shell`.
- Reason: they directly react to the strongest retained evidence pair in the exhausted lane. `screen_0004` showed the evaluator will reward stronger selective protection, while `screen_0007` and `screen_0009` showed that broad cheapening and under-filtered release destroy the weak slices and arb metrics. These two branches attack that bottleneck without replaying the dead split-side family.

## Execution Order

1. `vol-accumulator-side-tax`
2. `drawdown-budget-shell`
3. `recovery-corridor-auction`
4. `inventory-toxicity-triad`

## Queue Decision

- Queue `vol-accumulator-side-tax` first.
- Reason: it is the highest-leverage evidence-backed pivot that still preserves the anti-arb lesson from `screen_0004` while directly addressing the short-horizon state coupling behind both the over-spiky and too-cheap failures.

## Execution Log

- `2026-04-17 09:28 PDT`: reviewed `docs/agent_harness_guide.md`, `docs/hill_climb_loop.md`, `docs/codex_idea_generation_prompt.md`, `artifacts/index.json`, `artifacts/INDEX.md`, the `apr17-screen480-0834` read surfaces, two representative hypothesis payloads, two representative `compare-profiles` reads, and the retained seed-refresh research memo.
- `2026-04-17 09:30 PDT`: validated the current `contracts/src/Strategy.sol`, seeded the fresh retained lane as `screen_0001 @ 473.616393`, and confirmed the current source remains phenotypically identical to the prior incumbent baseline.
- `2026-04-17 09:31 PDT`: registered batch `apr17-screen480-0928-batch1`, recorded the `screen >= 480` breakout gate, and queued `vol-accumulator-side-tax` as the first unspent hypothesis.
- `2026-04-17 09:31 PDT`: re-read `status`, `show-hypothesis`, and `analyze-run` for the new lane. The harness reports four open hypotheses across all four primary layers, one explicit topology branch, no repeated quote topologies, and no portfolio gaps.
- `2026-04-17 17:17 PDT`: spent `vol-accumulator-side-tax-v1` as `screen_0002`. The branch failed immediately as a cheap-mode replay: `mean_edge = 346.501712`, `arb_loss_to_retail_gain = 0.390135`, `low_retail_mean_edge = 304.048342`, `low_volatility_mean_edge = 338.434682`, and `time_weighted_mean_fee = 0.001868806`.
- `2026-04-17 17:18 PDT`: narrowed the state mutation and spent `vol-accumulator-side-tax-v2` as `screen_0003`. The branch remained a broad underpricing failure with the same signature: `mean_edge = 369.787520`, `arb_loss_to_retail_gain = 0.350833`, `quote_selectivity_ratio = 170.365043`, and `time_weighted_mean_fee = 0.002059300`.
- `2026-04-17 17:20 PDT`: spent the third refinement `vol-accumulator-side-tax-v3` as `screen_0004`. It modestly improved on v2 but still failed hard versus baseline: `mean_edge = 371.629333`, `arb_loss_to_retail_gain = 0.347696`, `low_decile_mean_edge = 225.160717`, and `time_weighted_mean_fee = 0.002101567`.
- `2026-04-17 17:20 PDT`: restored the `screen` incumbent back into `contracts/src/Strategy.sol` with `pull-best` and re-ran `analyze-run`. The harness now marks mutation family `vol_accumulator_floor` as a dead end after 3 discards, closes the `state` branch with `actual_failure_mode = arb_leak_regression`, and leaves `drawdown-budget-shell`, `recovery-corridor-auction`, and `inventory-toxicity-triad` as the remaining open hypotheses.
- `2026-04-17 17:20 PDT`: next recommended spend is `drawdown-budget-shell`. The retained evidence says the state-memory pivot keeps collapsing into broad cheapening, so the next move should test explicit risk accounting rather than spend another iteration on filtered-floor semantics.
- `2026-04-17 17:34 PDT`: spent `drawdown-budget-shell-v1` as `screen_0005`. The first risk-budget rewrite failed as an extreme cheap-mode replay: `mean_edge = 300.964341`, `arb_loss_to_retail_gain = 0.462704`, `quote_selectivity_ratio = 223.381044`, `low_retail_mean_edge = 260.981258`, and `time_weighted_mean_fee = 0.002071367`.
- `2026-04-17 17:37 PDT`: refined the same family and spent `drawdown-budget-shell-v2` as `screen_0006`. The stronger floor and bilateral protection improved over v1 but still remained decisively underpriced versus baseline: `mean_edge = 332.587463`, `arb_loss_to_retail_gain = 0.410906`, `quote_selectivity_ratio = 165.593513`, `low_retail_mean_edge = 291.354573`, and `time_weighted_mean_fee = 0.002481415`.
- `2026-04-17 17:38 PDT`: restored the `screen` incumbent into `contracts/src/Strategy.sol` with `pull-best` and updated run state to queue `recovery-corridor-auction`. The harness guidance has now reached `pivot now` at 5 consecutive non-improving `screen` evaluations, and the new evidence says both the state and risk-budget pivots cheapen the book, so the next spend should keep the incumbent risk path fixed and test only a narrow non-toxic recovery corridor.
- `2026-04-17 17:41 PDT`: spent `recovery-corridor-auction-v1` as `screen_0007`. The first corridor variant stayed structurally live instead of collapsing into cheap mode: `mean_edge = 473.095646`, `arb_loss_to_retail_gain = 0.113009`, `low_decile_mean_edge = 361.599747`, and `quote_selectivity_ratio = 23.134274`, but it over-tightened the book, lifting `time_weighted_mean_fee` to `0.004884917` and giving back `low_retail_mean_edge` and `low_volatility_mean_edge`, so it remained a discard at `-0.520747` versus the incumbent.
- `2026-04-17 17:43 PDT`: refined the same family and spent `recovery-corridor-auction-v2` as `screen_0008`. Restoring the incumbent shared rebate skeleton plus a stricter corridor gate made the branch effectively inert: `mean_edge = 473.616338`, `arb_loss_to_retail_gain = 0.123562`, `quote_selectivity_ratio = 29.083079`, and every tracked slice was numerically indistinguishable from `screen_0001`. That closed the weak-slice pivot as exhausted for this lane.
- `2026-04-17 17:46 PDT`: restored the incumbent, updated run state to queue `inventory-toxicity-triad`, and spent `inventory-toxicity-triad-v1` as `screen_0009`. The tri-channel quote-map pivot produced the best raw result in the lane at `485.703883`, improving `arb_edge` to `-53.742882`, `arb_loss_to_retail_gain` to `0.099626`, `low_decile_mean_edge` to `370.458401`, `low_retail_mean_edge` to `415.627503`, `low_volatility_mean_edge` to `462.893208`, and `quote_selectivity_ratio` to `21.310587`, but it paid with higher `time_weighted_mean_fee = 0.004674950` and a small `retail_edge` giveback, so the `+12.087490` delta still missed the `20.305024` promotion margin and remained a discard.
- `2026-04-17 17:47 PDT`: restored the `screen` incumbent into `contracts/src/Strategy.sol`, cleared the stale queued hypothesis from run state, and re-read `status`. The lane now reports `Best Raw = screen_0009`, `Incumbent = screen_0001`, `Next Hypothesis = none`, and `Stop-Rule Guidance = stop now (8 consecutive non-improving screen evaluations; threshold 8)`, so `apr17-screen480-0928` is evidence-complete and should be treated as stopped retained history rather than an active mutation lane.
