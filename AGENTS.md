# Repo Working Notes

## Protected Benchmark

- `contracts/src/Reference.sol` is a protected benchmark source.
- Do not open, read, diff, summarize, compare against, or otherwise inspect that file unless the user explicitly authorizes access to `Reference.sol` in the current turn.
