# Context Log

- Prior rounds established that broad rewrites underperform; the best retained local line is the `V4` defense regime around `428`.
- Several prior ideas improved raw `mean_edge` but did not clear the promotion margin.
- The fresh round confirmed the live baseline is already above the `420` breakout target, so the next batch should optimize for incremental robustness, not wholesale redesign.
